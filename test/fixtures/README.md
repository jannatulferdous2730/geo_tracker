# test/fixtures/
# This folder holds saved sample API responses used in unit tests.
# Tests load these JSON files instead of making real network calls.
#
# Files added per phase:
#   Phase 7: overpass_hospitals.json  — Overpass response with node, way, and unnamed element
#   Phase 8: osrm_route.json          — OSRM route response with geometry, distance, duration
