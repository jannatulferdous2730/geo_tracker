// lib/services/storage_service.dart
// Reads and writes the saved-places JSON list to SharedPreferences.
// Takes SharedPreferences through the constructor so it can be faked in tests.
// Implemented in Phase 5.
